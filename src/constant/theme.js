import { color } from "./color";
import { space, lineHeight } from "./spacing";
import { size } from "./size";
import { font, fontWeight, fontSize } from "./font";

export const theme = {
  color,
  space,
  lineHeight,
  size,
  font,
  fontSize,
  fontWeight,
};
